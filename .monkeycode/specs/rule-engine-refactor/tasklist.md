# 需求实施计划

- [ ] 1. 创建新规则引擎核心架构
  - [ ] 1.1 创建 `internal/core/rule` 目录结构
    - 创建 `rule.go`: 定义统一规则模型 (UnifiedRule) 和规则来源枚举
    - 创建 `condition.go`: 定义条件表达式接口和 AST 结构
    - 创建 `matcher.go`: 定义匹配器接口和基础实现
  - [ ] 1.2 创建字段映射模块
    - 创建 `field_mapper.go`: 定义 FieldMapper 接口
    - 创建 `falco_mapper.go`: 实现 Falco 字段映射
    - 创建 `tracee_mapper.go`: 实现 Tracee 字段映射
  - [ ] 1.3 创建规则解析器接口
    - 创建 `parser.go`: 定义 RuleParser 接口
    - 定义 Parse() 方法签名和错误处理规范

- [ ] 2. 实现 Falco 规则解析器
  - [ ] 2.1 创建 Falco YAML 解析模块
    - 创建 `parsers/falco/parser.go`: FalcoParser 结构体
    - 实现 Falco 规则文件结构体 (FalcoRuleFile, FalcoRule, FalcoMacro, FalcoList)
    - 实现宏展开逻辑 (expandMacros)
  - [ ] 2.2 实现 Falco 条件表达式解析
    - 创建 `parsers/falco/condition_parser.go`
    - 实现条件表达式词法分析 (tokenize)
    - 实现 DNF 规范化处理 (buildDNFGroups)
    - 支持 AND/OR/NOT 逻辑运算符
  - [ ] 2.3 实现 Falco 比较运算符解析
    - 支持 =, !=, contains, icontains, startswith, endswith
    - 支持 in, not in, exists, not exists
    - 支持 >, <, >=, <= 数值比较
    - 支持 regex 正则匹配
  - [ ] 2.4 实现 Falco 规则转换
    - 将 FalcoRule 转换为 UnifiedRule
    - 处理 priority 到 severity 的映射
    - 处理 tags 到 MITRE ATT&CK 的映射
  - [ ]* 2.5 编写 Falco 解析器单元测试
    - 测试宏展开正确性
    - 测试条件表达式解析
    - 测试边界情况处理

- [ ] 3. 实现 Tracee 规则解析器
  - [ ] 3.1 创建 Tracee JSON 解析模块
    - 创建 `parsers/tracee/parser.go`: TraceeParser 结构体
    - 定义 Tracee 签名结构体 (TraceeSignature, TraceeSelector)
    - 实现 JSON 解析逻辑
  - [ ] 3.2 实现 Tracee 事件选择器解析
    - 解析 event/events 字段
    - 解析 selectors 结构
    - 处理事件名称规范化
  - [ ] 3.3 实现 Tracee 规则转换
    - 将 TraceeSignature 转换为 UnifiedRule
    - 处理 severity/level 字段映射
    - 保留原始签名元数据
  - [ ]* 3.4 编写 Tracee 解析器单元测试
    - 测试 JSON 解析正确性
    - 测试签名转换完整性

- [ ] 4. 实现条件匹配引擎
  - [ ] 4.1 创建表达式求值器
    - 创建 `matcher/evaluator.go`: ExpressionEvaluator 结构体
    - 实现 Evaluate() 方法执行条件表达式
    - 支持短路求值优化性能
  - [ ] 4.2 实现字段值解析器
    - 创建 `matcher/resolver.go`: FieldResolver 结构体
    - 实现 GetFieldValue() 方法从事件中提取字段值
    - 支持嵌套字段访问 (如 dst_addr.ip)
    - 支持计算字段 (如 event_type 别名)
  - [ ] 4.3 实现比较运算符执行
    - 创建 `matcher/comparators.go`: 比较运算符实现
    - 实现字符串比较 (equals, contains, regex)
    - 实现数值比较 (gt, lt, gte, lte)
    - 实现集合运算 (in, not_in)
    - 实现存在性检查 (exists, not_exists)
  - [ ] 4.4 实现匹配结果缓存
    - 创建 `matcher/cache.go`: MatchCache 结构体
    - 实现基于事件特征哈希的缓存
    - 设置缓存过期和清理策略
  - [ ]* 4.5 编写匹配引擎单元测试
    - 测试各种比较运算符
    - 测试逻辑运算符组合
    - 测试性能基准

- [ ] 5. 重构规则引擎主模块
  - [ ] 5.1 创建新的 RuleEngine 结构
    - 创建 `engine.go`: NewRuleEngine() 工厂函数
    - 支持多解析器注册 (RegisterParser)
    - 支持热加载规则文件
  - [ ] 5.2 实现规则编译流程
    - 实现 CompileRules() 方法
    - 预编译正则表达式
    - 构建规则索引加速匹配
  - [ ] 5.3 实现规则匹配流程
    - 实现 MatchRules(event) 方法
    - 应用白名单过滤
    - 应用节流机制
    - 生成告警事件
  - [ ] 5.4 实现规则统计功能
    - 记录规则触发次数
    - 记录匹配耗时
    - 提供统计查询接口
  - [ ] 5.5 集成事件上下文
    - 在匹配时更新 EventContext
    - 关联攻击链检测
    - 映射 MITRE ATT&CK 技术

- [ ] 6. 创建规则导入工具
  - [ ] 6.1 创建命令行工具
    - 创建 `cmd/etracee-import/main.go`
    - 支持 -format 参数 (falco/tracee/auto)
    - 支持 -input 和 -output 参数
    - 支持 -validate 参数验证规则
  - [ ] 6.2 实现批量导入功能
    - 支持导入 Falco 官方规则库
    - 支持导入 Tracee 签名库
    - 处理规则冲突和去重
  - [ ] 6.3 实现规则验证功能
    - 验证规则语法正确性
    - 验证字段映射完整性
    - 生成验证报告

- [ ] 7. 检查点 - 验证规则引擎功能
  - 运行 Falco 规则导入测试
  - 运行 Tracee 规则导入测试
  - 验证告警生成正确性

- [ ] 8. 迁移现有规则和配置
  - [ ] 8.1 更新配置文件格式
    - 更新 `config/enhanced_security_config.yaml`
    - 添加 source 字段标识规则来源
    - 添加 enabled 字段控制规则启用
  - [ ] 8.2 转换现有规则
    - 将现有规则转换为新格式
    - 保留原有规则的语义
    - 添加缺失的字段映射
  - [ ] 8.3 更新 main.go 集成
    - 替换旧的 EnhancedRuleEngine
    - 使用新的 RuleEngine 接口
    - 保持向后兼容性

- [ ] 9. 性能优化
  - [ ] 9.1 实现规则索引
    - 按事件类型索引规则
    - 按严重级别索引规则
    - 按字段索引规则
  - [ ] 9.2 实现匹配优化
    - 使用 Bloom Filter 快速过滤
    - 实现批量匹配优化
    - 优化内存分配
  - [ ] 9.3 实现并行匹配
    - 使用 goroutine 并行匹配
    - 控制 goroutine 池大小
    - 处理并发安全问题
  - [ ]* 9.4 编写性能测试
    - 编写基准测试用例
    - 对比新旧引擎性能
    - 生成性能报告

- [ ] 10. 文档和测试
  - [ ] 10.1 编写 API 文档
    - 编写规则格式文档
    - 编写字段映射文档
    - 编写解析器接口文档
  - [ ] 10.2 编写用户指南
    - 编写规则导入指南
    - 编写自定义规则指南
    - 编写故障排查指南
  - [ ]* 10.3 编写集成测试
    - 编写端到端测试
    - 测试与 eBPF 模块集成
    - 测试与 Web API 集成
